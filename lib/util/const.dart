const TitleFont = "Billabong";
const BoldFont = "NotoSansJP-Bold";
const RegularFont = "NotoSansJP-Medium";


enum PostUploadType { GALLERY, CAMERA }

enum PostCaptionOpenMode {
  FROM_POST,
  FROM_FEED,
}

enum FeedMode {
  FROM_FEED,
  FROM_PROFILE,
}
